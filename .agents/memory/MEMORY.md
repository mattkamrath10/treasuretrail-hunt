- [Mobile architecture rules](mobile-architecture-rules.md) — pointer to repo-root `ARCHITECTURE.md` covering scroll/detail/deep-link/safe-area/image/external-link rules; fix the shared helper, not the call site.
- [Supabase select schema drift](supabase-select-schema-drift.md) — one missing column 400s the whole `.select()`; soft fallbacks then hide it as "X unavailable" on every row.
- [Event image bug — toThumbUrl double-process](event-image-thumb-double-process.md) — `toThumbUrl(cover_thumb_url)` rewrites already-thumb URLs into `.thumb.thumb.jpg` (404s); always pass the raw stored thumb to <img>, or make toThumbUrl idempotent.
- [Mobile detail-page containment](mobile-detail-page-containment.md) — wrap every detail route in <MobileDetailPage>; never put touch-action:pan-y on html/body or PageScroll (kills horizontal carousels on iOS).
- [Discover carousel wheel behavior](discover-carousel-wheel.md) — plain wheel scrolls the PAGE, only shift+wheel scrolls a Discover row; never re-add vertical-wheel→horizontal hijack. Desktop=hover arrows, mobile=swipe+progress bar.
- [Supabase image upload pipeline](supabase-image-upload.md) — uploads go to public `avatars` bucket, path `${userId}/${folder}/${timestamp}.jpg` + `.thumb.jpg`; RLS keys on `name LIKE auth.uid()||'/%'`; thumb upload failure is non-fatal and only console.warns.
- [Migration-gated fetcher pattern](migration-gated-fetcher-pattern.md) — when a public fetcher gains a column-dependent filter, ship a `build(withFilter)` closure + 42703-retry scoped to the column name; agent cannot apply Supabase DDL, user applies migrations manually via SQL editor.
- [Supabase confirm-email delivery (Apple 2.1a)](supabase-confirm-email-delivery.md) — "no confirmation email" = Supabase built-in emailer sets confirmation_sent_at but doesn't deliver; RESOLVED via Resend custom SMTP (host smtp.resend.com:465, user resend); fix is dashboard, not signup code.
- [treasuretrail-hunt.com DNS lives in Replit, not Name.com](supabase-confirm-email-delivery.md) — domain is *Registered With: Replit*; edit DNS in Publishing→Domains→Manage despite name.com nameservers; apex has strict DMARC p=reject so any sender needs aligned SPF+DKIM.
- [Firebase messaging iOS launch crash](firebase-messaging-launch-crash.md) — @capacitor-firebase/messaging self-runs FirebaseApp.configure() in its iOS load() at launch; missing GoogleService-Info.plist hard-crashes the app on open despite fully-guarded push.ts. Fix: add the plist, or drop the plugin.
- [Codemagic iOS TestFlight no-Mac signing](codemagic-ios-testflight-no-mac.md) — Mac-less signing recipe: generate key + pass --certificate-key to fetch-signing-files, but NOT to keychain add-certificates; 401 = ASC API key in Codemagic UI, not code.
- [Capacitor packaging constraints](capacitor-packaging-constraints.md) — prod is single-domain Autoscale (one Express server serves dist + /api; dev keeps 3001 via PORT fallback for the Vite proxy); router must be platform-conditional (HashRouter native / BrowserRouter web), not a blanket swap.
- [Go-live native push fan-out](go-live-push-fanout.md) — server push claim must mirror in-app RPC eligibility EXACTLY (incl. live-window upper bound) and release its `go_live_pushed_at` claim on wholly-transient FCM failure to avoid permanent suppression.
- [Modal scroll lock](modal-scroll-lock.md) — background scroll lock is `useScrollLock` (document touchmove/wheel + `[data-scroll-lock-allow]`), not CSS body lock; scroller is PageScroll div, never set touch-action.
- [Go-live dual-claim fan-out](go-live-dual-claim-fanout.md) — each go-live channel (in-app, push) gets its OWN events.*_at atomic-claim column so one channel's failure can't suppress another; recipients always derived server-side from followers.
- [Branded image-fallback rule](branded-image-fallbacks.md) — every optional `<img>` must go through `<ImageWithFade fallback={<MediaFallback/AvatarFallback/>}/>`; raw img + gray placeholder = regression. Pass kind/platform/seed.
- [ImageWithFade cached-image race](imagewithfade-cached-race.md) — cached img can fire onLoad before React attaches it → stuck opacity:0 → fallback shows over a valid 200 URL; fix checks img.complete. Don't assume a persistent fallback means a bad URL.
- [Build runs strict tsc](build-tsc-noUnusedLocals.md) — `npm run build` = tsc+vite with noUnusedLocals; unused imports/params are HARD build failures invisible to the vite dev server.
- [Login is a conditional screen, not a route](auth-gate-not-a-route.md) — no /login route; navigate('/login') dumps guests on Discover. To gate: setPendingIntent + exitGuestMode + navigate('/'); resume in AppShell.useResumePendingIntent.
- [Live Events vs Discover datasets](livehub-discover-dataset-split.md) — hosted events live in `events` (Discover + boost); `/live` historically only read `external_listings`, so boosted hosted events vanished there. LiveHub now merges both via eventToListing.
- [Escalation-guard privileged columns](escalation-guard-privileged-columns.md) — every paid/trust/role column must be added to the BEFORE INSERT OR UPDATE escalation guard when created; RLS gates the row, the trigger gates the column; grants go through service-role server/grants.ts.
- [Reach analytics data-gate](reach-analytics-data-gate.md) — Pro analytics gated in DB: definer RPC `fetch_seller_reach` (Pro+ownership) is sole path; count views' authenticated SELECT revoked. UI-only gating isn't an entitlement.
- [Go-live notification dedupe](go-live-notification-dedupe.md) — fan-out alerts dedupe server-side via atomic claim UPDATE (...notified_at IS NULL...RETURNING) inside a SECURITY DEFINER RPC; client `attempted` set is only a same-session optimization, never the real gate.
- [Autoscale vs publicDir conflict](autoscale-publicdir-conflict.md) — Static→Autoscale switch leaves `publicDir` in .replit (disables Publish / stays Static); deployConfig wont remove it, user must hand-edit .replit.
- [Native /api fetch rule](native-api-fetch-rule.md) — relative `fetch('/api')` breaks in Capacitor webview; every /api call must use `apiUrl()`, links use `publicWebUrl()`; VITE_API_BASE is a build-time var.
- [cap add ios on Linux](capacitor-ios-on-linux.md) — Capacitor 8 CLI needs Node>=22; `cap add ios` scaffolds with SPM (no CocoaPods/Mac) so the ios/ project generates fine on Replit.
- [Saved Finds dual store](saved-finds-dual-store.md) — saves live in BOTH localStorage `tt_saved_posts` (community finds) and DB `saved_listings`; any "my saves" UI must read both via fetchSavedFinds().
- [Native build missing VITE_ env](native-build-missing-vite-env.md) — Codemagic build doesn't inherit Replit secrets; any missing client VITE_ var (esp. SUPABASE_URL/ANON) = blank-screen launch since supabase.ts createClient throws at import before ErrorBoundary.
- [Stale index.html cache masks deploys](stale-index-html-cache.md) — "still broken after 4 fixes" can be a cached SPA entry point on mobile Safari, not a code/DB bug; serve index.html no-cache, /assets immutable.
- [iOS bottom-sheet dvh](ios-bottom-sheet-dvh.md) — bottom-anchored sheets sized with vh hide their lowest control (Delete Account) behind the mobile Safari toolbar; size overlay+sheet with dvh (vh fallback) via .tt-modal-overlay/.tt-sheet classes.
- [Events location search](events-location-search.md) — Events search is a LOCATION filter (ZIP->zippopotam, City,State->nominatim, both keyless/CORS/absolute-URL for webview); haversine <=100mi, null lat/lng hidden, no DB migration.
- [User location store](user-location-store.md) — personalized user location is client-side only (`localStorage tt_user_location` via `userLocation.ts` + `useSavedLocation()` useSyncExternalStore); no profiles coord column, no migration. Reuse for any nearby feature.
- [Profile Settings removed](profile-settings-removed.md) — the un-scrollable Profile Settings modal was deleted at user's request; Delete Account + legal links now live in an "Account" section inside the scrollable Profile page (PageScroll), not a sheet. Don't re-add a settings modal.
- [Block needs reachable unblock](block-needs-unblock-list.md) — blocking filters the user out of every feed/profile, so the toggle button is unreachable; any block feature MUST ship a standalone Blocked Users list (Profile→Account → /blocked) as the only unblock path.
- [Profile Finds = posts + listings](profile-finds-dual-source.md) — the profile "Finds" stat sums community_posts + marketplace_listings; any per-user finds UI must read BOTH or it under-counts (a 1-post/many-listing user showed only 1 item).
- [iOS monetization-hide flag (now OFF)](ios-monetization-hide-flag.md) — monetizationHidden() AND iosPaymentsBlocked() both return false now; Pro/boost/IAP are LIVE everywhere via RevenueCat. Pro.tsx "payments disabled until Stripe" comment is STALE. Switches still wired if re-enabled.
- [Android AAB built in Android Studio, not Codemagic](android-aab-codemagic.md) — user builds .aab locally in Android Studio (no Codemagic sub for Android); Replit can't build it. applicationId MUST be com.treasuretrailhunt (no dot).
- [iOS camera/photo crash](ios-camera-plist-crash.md) — webview file inputs (Take Photo / library) HARD-crash on iOS without NS*UsageDescription keys in ios/App/App/Info.plist; adding the input isn't enough.
- [Play already has a registered upload key](play-existing-upload-key.md) — com.treasuretrailhunt (no dot) already published once with an upload key (SHA1 76:E3:EC...) created OUTSIDE this repl; never assume "first upload, any new keystore works." A freshly-generated keystore is rejected; recover original or do Play upload-key reset.
- [Guest gate keys on !user not !profile](guest-gate-not-profile.md) — auth-only pages must show AccountRequired for guests first; `!profile` spinner hangs forever since guests never get a profile.
- [App Store screenshot generation](appstore-screenshot-generation.md) — render dev server in Nix-chromium headless at deviceScaleFactor; skip onboarding via localStorage, enter guest then navigate client-side (pushState+popstate) since goto reloads reset guest state; flatten PNG alpha for ASC.
- [Pro included boost path](pro-included-boost.md) — Pro boosts must route to free /api/boost/pro (server re-verifies tier), never the paid IAP sheet; every boost CTA branches isPro for BOTH label and action.
- [iOS safe-area needs viewport-fit=cover](ios-safe-area-viewport-fit.md) — env(safe-area-inset-*) silently = 0 without viewport-fit=cover in index.html viewport meta; headers sit under status bar in portrait, fine in landscape. Per-page inset is not centralized.
- [PostgREST 0-row UPDATE = success](postgrest-zero-row-update-success.md) — grant/revoke/boost writers must `.select('id')` and fail on 0 rows, else mismatched app_user_id silently keeps users free/Pro; webhook user id falls back to original_app_user_id.
- [Codemagic firewall lockfile URLs](codemagic-replit-firewall-lockfile.md) — Replit pkg installs poison package-lock.json with package-firewall.replit.local URLs; external npm install (Codemagic) 500s ENOTFOUND. sed them to registry.npmjs.org, push, never `npm install` to verify.
- [Service-role boost/grant ownership](service-role-boost-ownership.md) — service-role bypasses RLS, so server must re-check target ownership AND treat 0-row PostgREST updates as failure before consuming a paid claim.
- [LIKE-escape user text in matchers](like-escape-user-text-matchers.md) — ILIKE substring matchers built from user text must escape %/_/\ (ESCAPE '\') or `%%%` matches everything = spam; pair with claim-ledger + EXCEPTION guard + jsonb_typeof pref gate.
- [Wanted-response notification flow](wanted-response-notification-flow.md) — wanted-reply alert fires ONLY via the wanted_responses insert trigger (sendMessage has no notif); notify_user has a hard DB allowlist, per-user category prefs gate in-app CLIENT-side.
- [Demand intelligence privacy + gating](demand-intelligence-privacy.md) — search_demand aggregates (term,category,~7mi geocell); writes via definer record_search_demand (RLS no-policy table), reads Pro-gated definer RPCs; never store/expose individuals.
- [Search aggregator ordering + external seam](search-aggregator-ordering.md) — local-first order TT(near/more)→events→external(near/shipping)→Google; external providers run concurrently + soft-fail, NOT a waterfall; demand-capture fires on total emptiness (breaks once external enabled).
- [Recurring events read-boundary](recurring-events-readboundary.md) — one anchor row; normalize next occurrence at read (fetchPublishedEvents/followFeed); raw-row pages (EventDetail) must applyNextOccurrence AND derive live/soon/expired from it, not the anchor.
- [AI Wanted Wizard fallback](ai-wizard-fallback-pattern.md) — AI category/question gen sits behind Phase 2 interfaces; server returns {fallback:true} HTTP 200 on any failure, client always has rule-based/static fallback, AI questions only swap in while still on wizard step 0; key stays server-side via apiUrl()+bearer.
- [Transactional push fan-out](transactional-push-fanout.md) — message/wanted/follow/listing push claims on ONE notifications.pushed_at column; recipient derived server-side from claimed row, actor=caller JWT gate; push pref defaults ON in both client+server; DMs now create one notif+push per message.
- [iPad responsive width tokens](ipad-responsive-width-tokens.md) — widen `--max-width` at 700/1024px media queries to scale the whole app; fixed FABs offset by `--side-gutter`, toasts cap width to `--max-width`.
- [Marketplace create beta flag](marketplace-create-flag.md) — `MARKETPLACE_CREATE_ENABLED` now lives in `src/lib/featureFlags.ts` as the SINGLE source of truth gating BOTH manual Create-Listing AND Smart Screenshot Import publish; never re-add a local copy.
- [Businesses on the Treasure Map](businesses-treasure-map.md) — Business entity mirrors events; map page is still EventsMap.tsx but renders "Treasure Map" with TWO cluster layers (events+businesses); 8 categories + verified/featured DB-gated.
- [Native push plugin import must be bundled](push-plugin-import-bundling.md) — load Capacitor plugins with a literal `await import(...)`, never @vite-ignore + variable specifier, or push silently no-ops on device.
- [Event/item deletion storage cleanup](event-deletion-storage-cleanup.md) — featured-item rows cascade-delete; only storage images need manual cleanup (best-effort .remove BEFORE row delete, matching deletePost); a retry queue was deliberately declined. All destructive UI uses ui/ConfirmDialog.tsx.
- [Discover boost-first ranking contract](discover-boost-ranking-contract.md) — boost outranks content KIND (paid>pro>featured>normal) so paid promotion always lifts; event_featured_items surface in Discover as find slides; location is still a hard radius filter even for boosted.
- [Generate large files in /tmp not public/](generate-large-files-in-tmp.md) — archiving into a vite-watched dir stalls at 0 bytes (looks like OOM 137); build in /tmp then cp.
- [MediaFallback label doubling](mediafallback-label-doubling.md) — don't pass label={title} to a MediaFallback used as ImageWithFade fallback when the card also draws its own caption title; the title renders twice on image-less items.
- [Discover Featured slideshow](discover-featured-slideshow.md) — Discover top is a unified FeaturedSlideshow over events/businesses/wanted/finds; ranking in lib/discoverFeatured.ts; Discover is LIGHT (--tt-* tokens); chip persisted (tt_discover_filter); finds geocoded at read-time (geocodeCached); businesses fetch soft-fails (PGRST205) pre-migration.
- [Discover category rows + per-event OG](discover-category-rows-and-og.md) — themed Discover sub-rows MUST be kind-guarded before category/keyword (event-items leak as finds); shared /event links unfurl ONE card via server OG injection + imageUrl:null share.
- [Light theme semantic tokens](light-theme-tokens.md) — `--tt-*` token system (light :root + `[data-theme="dark"]` scope, orange accent); Wanted trio converted green→orange; Discover is now LIGHT too; UserShowcase Rail accents are a per-category legend, don't recolor to orange.
